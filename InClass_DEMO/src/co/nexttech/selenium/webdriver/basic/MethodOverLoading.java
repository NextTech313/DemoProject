package co.nexttech.selenium.webdriver.basic;

public class MethodOverLoading {
	
	/*Method OverLoading is - when you need more then one method with the same name 
	 * in the same class. To use method overloading - 1)The method name must be same
	 * 2)The Parameter must be different-> you could make the parameter different 
	 * in 3 ways >> 1)Size of the parameters 2)Type of the parameters and 
	 * 3) Sequence of the parameter. There are some flexibility-Access modifier and Return type 
	 * could be same or different.
	 */

}
