package co.nexttech.selenium.webdriver.basic;

import org.openqa.selenium.chrome.ChromeDriver;

public class SeleniumIntro {

	public static void main(String[] args) {
		
		ChromeDriver driver = new ChromeDriver();
		
		//Once you write the above line-You will have to import the package shown on line3.
		//driver Object here has access to all the methods of ChromeDriver. 
		//Methods - OpenBrowser, CloseBrowser, Click anything, Use Scroll bar, Minimize and Maximize window etc.
		//Chrome - ChromeDriver -> methods are same
		//Firefox - FirefoxDriver -> methods are same
		//Safari - SafariDriver -> methods are same
		
	}

}
